/**
 * How to export multiple functions, objects, or primitive values from the module in JavaScript
 */


//There are two different types of export, named and default. You can have many named exports but only one default export.
export { cube, foo, graph };

export default function myFunction() {  }